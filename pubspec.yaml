name: me_indik_app
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^4.0.0
  dartz: ^0.9.2
  intl: ^0.17.0

dev_dependencies:
  flutter_config: ^1.0.8
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
